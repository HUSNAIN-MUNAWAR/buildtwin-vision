# Contributing

Use focused branches and include tests for domain changes. Do not add hardcoded dashboard values, silent fallbacks, unlicensed media, committed secrets, or model claims without reproducible evidence. Run backend tests/lint/type checks and frontend test/lint/typecheck/build before opening a pull request. Migrations must be forward/backward reviewed and organization scoping must be tested.
